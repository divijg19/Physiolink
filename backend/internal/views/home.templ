package views

templ Home(name string, isLoggedIn bool) {
	@Layout("Home", isLoggedIn) {
		<h1 class="text-2xl font-bold mb-4">Welcome, { name }!</h1>
		<p>This is the Physiolink public page.</p>
		<button class="bg-blue-500 text-white px-4 py-2 rounded">
			Click Me (HTMX demo)
		</button>
	}
}
